name: Deploy
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del repositorio
      uses: actions/checkout@v3

    - name: Paso básico de CI
      run: echo "CI funcionando"